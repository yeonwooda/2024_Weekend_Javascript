const colorList = ["red", "orange", "yellow", "green", "blue"];

colorList.forEach((v) => {
  const colorBox = document.createElement(".box");
});
// colorBox.addEventListener("click", () => {
//     alert("")
// })
// css: display - grid / grid-template-columns: repeat(5, 1fr)
